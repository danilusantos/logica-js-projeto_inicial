// 1
let contadorMais = 1;

while (contadorMais <= 10) {
    console.log(contadorMais);
    contadorMais++;
}

// 2
let contadorMenos = 10;

while (contadorMenos >= 0) {
    console.log(contadorMenos);
    contadorMenos--;
}

// 3

let contagemRegressiva = prompt('Digite um número para a contagem: ');

while (contagemRegressiva >= 0) {
    console.log(contagemRegressiva);
    contagemRegressiva--;
}

// 4

let contagemProgressiva = prompt('Digite um número para a contagem: ');
let contador = 0;

while (contador <= contagemProgressiva) {
    console.log(contador);
    contador++;
}

